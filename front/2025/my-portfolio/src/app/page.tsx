import Intro from "@/components/Intro";

export default async function Home() {
  return (
    <main className="relative w-full min-h-screen bg-gray-100 dark:bg-black">
      <Intro  />
    </main>
  );
}
